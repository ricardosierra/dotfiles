cat <<-HELP
Vcs Alias:
    gs: git status
    gu: git pull
    gl: git log
    gd: git diff
    gc: git checkout
    gcb: git checkout -b
    gr: git remote
    gbs: Current Branch
    gurlp: GitHub URL for current repo, including current branch + path.

Vcs Functions
    ged: open all changed files
    gra: add a github remote by github username
    gurl: GitHub URL for current repo.
    gfu: open last commit in GitHub, in the browser.
    gpu: open current branch + path in GitHub, in the browser.
    gf: git log with per-commit cmd-clickable GitHub URLs (iTerm)
    gstat: Combine diff --name-status and --stat
    gitio: Create a git.io short URL
    repo: Call from a local repo to open the repository on github/bitbucket in browser

HELP
